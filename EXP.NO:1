AIM
To write a program that implements the Caesar Cipher technique, where each alphabet in the plaintext is shifted by k positions to generate the ciphertext. This value k ranges from 1 to 25.
#include <stdio.h>
#include <ctype.h>

int main() {
    char text[100];
    int k;

    printf("Enter a string: ");
    fgets(text, sizeof(text), stdin);

    printf("Enter shift (1-25): ");
    scanf("%d", &k);

    for (int i = 0; text[i] != '\0'; i++) {
        char c = text[i];

        if (isupper(c)) {
            c = (c - 'A' + k) % 26 + 'A';
        }
        else if (islower(c)) {
            c = (c - 'a' + k) % 26 + 'a';
        }

        text[i] = c;
    }

    printf("Encrypted Text: %s", text);

    return 0;
}
The Caesar Cipher program was successfully implemented in both C and Python. The program correctly encrypts alphabetic characters by shifting them k positions (1–25) forward in the alphabet while preserving case. Non-alphabet characters remain unchanged.
